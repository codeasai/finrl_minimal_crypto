# Advanced Crypto Agent Requirements
# ติดตั้งด้วยคำสั่ง: pip install -r requirements_advanced.txt

# Core dependencies (จำเป็น)
pandas>=1.3.0
numpy>=1.21.0
matplotlib>=3.4.0
yfinance>=0.1.70
torch>=1.9.0
scikit-learn>=1.0.0

# FinRL
finrl

# Technical Indicators (เลือก 1 ใน 2)
# วิธีที่ 1: pandas_ta (แนะนำ - ติดตั้งง่าย)
pandas_ta>=0.3.14b

# วิธีที่ 2: TA-Lib (สำหรับ advanced users)
# TA-Lib-Binary  # สำหรับ Windows
# TA-Lib         # สำหรับ Linux/Mac

# Optional dependencies (ปรับปรุงประสิทธิภาพ)
# tensorboard     # สำหรับ monitoring การเทรน
# plotly          # สำหรับกราฟ interactive 